{
  "name": "04-policy-management",
  "hypothesis": "Realm setup for the 04-policy-management Playwright test: a realm role 'TestRole' exists, the test-app admin holds it, and a public test-app client is registered, so a GenericResourceAccessThresholdRole policy can later be created for that role; the baseline token still ORK-signs.",
  "tags": ["realm-setup", "policy", "ork"],
  "setup": [
    { "kind": "role.create", "args": { "name": "TestRole", "description": "Role the threshold policy is created for (04-policy-management)" } },
    { "kind": "client.create", "args": { "clientId": "testapp", "publicClient": true, "standardFlowEnabled": true, "directAccessGrantsEnabled": true, "redirectUris": ["http://localhost:3000/*"], "webOrigins": ["http://localhost:3000"], "attributes": { "pkce.code.challenge.method": "S256", "post.logout.redirect.uris": "+" } } },
    { "kind": "user.create", "args": { "username": "admin", "password": "Passw0rd!" }, "as": "admin" },
    { "kind": "user.assignRealmRole", "args": { "user": "$admin", "role": "TestRole" } }
  ],
  "probe": {
    "kind": "token.request",
    "args": { "grant": "password", "clientId": "admin-cli", "user": "$admin", "password": "Passw0rd!" }
  },
  "expect": { "side": "ork", "outcome": "sign" },
  "_backs": "tests/specs/04-policy-management.spec.js",
  "_provides": "realm role 'TestRole'; the test-app admin holds it; a public 'testapp' client (origin http://localhost:3000).",
  "_tideSetup": {
    "appClient": "testapp",
    "appLoginUser": "admin",
    "linkUsers": ["admin"],
    "realmAdmins": ["admin"]
  }
}
